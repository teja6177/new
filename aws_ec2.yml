plugin: amazon.aws.aws_ec2

regions:
  - ap-south-1

filters:
  instance-state-name: running
  tag:Environment: dev   

keyed_groups:
  - key: tags:Role
    prefix: tag

hostnames:
  - public-ip-address

compose:
  ansible_host: public_ip_address
