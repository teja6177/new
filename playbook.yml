- name: Provision EC2 instance
  hosts: localhost
  connection: local
  gather_facts: no

  roles:
    - ec2


- name: Configure DNS server
  hosts: tag_Role_dns
  become: yes
  gather_facts: yes

  roles:
    - dns
